name: Backend CI Pipeline

# Trigger: Run this robot whenever code is pushed to the 'main' branch
on:
  push:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest # The robot uses a Linux machine
    
    steps:
    # Step 1: Download your code
    - uses: actions/checkout@v3
    
    # Step 2: Install Python
    - name: Set up Python 3.9
      uses: actions/setup-python@v4
      with:
        python-version: "3.9"
        
    # Step 3: Install your libraries to make sure they work
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install fastapi uvicorn python-multipart
        
    # Step 4: Run a "Lint" check (Checks for syntax errors)
    - name: Check for Syntax Errors
      run: |
        python -m compileall backend/